import SearchForm from "./SearchForm";

export default function SearchSection() {
  return (
    <section id="search" className="py-20 bg-white scroll-mt-24">
      <SearchForm />
    </section>
  );
}
